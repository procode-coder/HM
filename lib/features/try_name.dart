import 'package:flutter/material.dart';

class TryName extends StatelessWidget {
  const TryName({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
